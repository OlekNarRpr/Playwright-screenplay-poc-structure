export const searchBar = {
  buttonMoreFilters:
    "//label[contains(text(), 'More Filters')]/following-sibling::button[@type='button']",
};

export const moreFilters = {
  buttonReset: "//button[@type='button'][contains(text(), 'Reset')]",
  buttonUpdateAndSearchTop:
    "//button[@type='button'][contains(text(), 'Reset')]/following-sibling::button[@type='submit'][contains(text(), 'Update and Search')]",
};
